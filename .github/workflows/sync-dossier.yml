name: Sync conformance dossier

# Keeps report.html in step with the dossier generated in eduardstan/reasonsmith.
# The canonical file is docs/report.html after PR #34 merges, docs/index.html before.
# A push made with the default GITHUB_TOKEN never triggers another workflow, so
# publish.yml does not fire for a sync commit — this workflow calls the deploy
# hook itself instead.

on:
  schedule:
    - cron: "17 * * * *" # hourly at :17
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fetch dossier from reasonsmith main
        run: |
          # Only docs/report.html is canonical; it exists on main once PR #34 merges.
          # Until then a 404 means "nothing new to publish" — do not fall back to the
          # legacy docs/index.html, which is the pre-restyle report.
          if ! curl -fsSL https://raw.githubusercontent.com/eduardstan/reasonsmith/main/docs/report.html -o report.html.new; then
            echo "no canonical dossier on main yet (PR #34 pending); skipping"
            exit 0
          fi
          if cmp -s report.html.new report.html; then
            echo "unchanged"
            rm report.html.new
          else
            mv report.html.new report.html
            echo "changed=true" >> "$GITHUB_ENV"
          fi

      - name: Commit and push if changed
        if: env.changed == 'true'
        run: |
          git config user.name "reasonsmith-site bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add report.html
          git commit -m "chore: sync dossier from eduardstan/reasonsmith@main"
          git push

      - name: Trigger the Vercel deploy hook
        if: env.changed == 'true'
        env:
          HOOK: ${{ secrets.VERCEL_DEPLOY_HOOK }}
        run: |
          # Fail loudly rather than reporting a green run that published nothing.
          if [ -z "$HOOK" ]; then
            echo "::error::VERCEL_DEPLOY_HOOK is not set - the synced dossier was committed but not published."
            exit 1
          fi
          curl -fsS -X POST "$HOOK"
